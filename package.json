{
  "name": "ims",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron . --inspect",
    "build_ui": "cd ims-app && ng build --prod && cd ..",
    "build": "npm run build_ui && node build.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nedb": "^1.8.0"
  },
  "devDependencies": {
    "@angular/cli": "^6.2.4",
    "electron": "^3.0.2",
    "electron-packager": "^12.2.0"
  }
}
