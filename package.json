{
  "name": "ims",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron . --inspect",
    "build": "electron-packager . --platform=win32 --arch=ia32 --out=.\\binaries"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nedb": "^1.8.0"
  },
  "devDependencies": {
    "@angular/cli": "^6.2.1",
    "electron": "^2.0.8",
    "electron-packager": "^12.1.2"
  }
}
