import { PrecisionDirective } from './precision.directive';

describe('PrecisionDirective', () => {
  it('should create an instance', () => {
    const directive = new PrecisionDirective();
    expect(directive).toBeTruthy();
  });
});
